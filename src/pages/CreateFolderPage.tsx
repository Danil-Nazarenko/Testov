import { Box, Typography } from '@mui/material';

const CreateFolderPage = () => {
  return (
    <Box>
      <Typography variant="h4">Создать папку</Typography>
      <Typography>Форма для создания новой папки.</Typography>
    </Box>
  );
};

export default CreateFolderPage;
