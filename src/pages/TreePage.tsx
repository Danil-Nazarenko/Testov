import { Box, Typography } from '@mui/material';

const TreePage = () => {
  return (
    <Box>
      <Typography variant="h4">Дерево</Typography>
      <Typography>Здесь будет дерево папок.</Typography>
    </Box>
  );
};

export default TreePage;
