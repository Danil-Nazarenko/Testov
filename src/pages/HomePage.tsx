import { Box, Typography } from '@mui/material';

const HomePage = () => {
  return (
    <Box>
      <Typography variant="h4">Главная</Typography>
      <Typography variant="body1">Добро пожаловать на главную страницу!</Typography>
    </Box>
  );
};  

export default HomePage;
